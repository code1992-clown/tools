---
alwaysApply: true
---

# [CRITICAL] 每次回复必须调用 interactive_feedback

**这是只读的反馈收集工具，所有模式（Agent / Plan / Ask）都必须调用，无例外。**

## 调用方式

- MCP 服务器：`user-mcp-feedback-enhanced`
- 工具名称：`interactive_feedback`
- 必传参数：
  - `project_directory`：项目路径（`/Users/bob.wang/workspace/project/amp-mkt-apollo-fe`）
  - `summary`：中文工作摘要（包含做了什么、改了哪些文件、解决了什么问题）
  - `timeout`：`3600`（秒，即 1 小时）

## 强制规则

1. **每次回复结束前**必须调用，不管任务是否完成、回复内容是否简单
2. **Plan 模式同样必须调用**——`interactive_feedback` 是只读工具，不受「禁止非只读工具」限制
3. 收到非空反馈 → 根据反馈执行调整 → 再次调用，循环直到用户说「结束 / 完成 / 不需要了」
4. 工具超时 → 在回复中说明「已尝试收集反馈，您可直接在对话中回复」
5. 用户提供图片时，必须分析图片内容并纳入工作上下文

## 自检清单（每次回复前）

- [ ] 本次是否已调用 `interactive_feedback`？未调用 → 立即调用
- [ ] `summary` 是否包含具体的工作内容？过于笼统 → 补充细节
- [ ] 收到用户反馈是否已处理？未处理 → 先处理再回复

## 禁止事项

- ❌ 以「任务简单」「只是回答问题」「已经完成」为由跳过调用
- ❌ 在未调用工具的情况下结束回复
- ❌ 忽略用户反馈内容

